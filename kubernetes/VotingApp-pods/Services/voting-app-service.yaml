apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name: voting-service
    app: demo-voting-app
spec:
  # Need to be externally accessible, so type = "LoadBalancer"
  type: LoadBalancer
  selector:
    name: voting-app-pod
    app: demo-voting-app
  ports:
    - port: 80
      targetPort: 80
